- plone mit instance debug starten
  dann kann man mit dem app object auf di zoperoot
  und damit änderungen in der db verspeichert werden
  import transaction
  dann transaction.commit() = damit werden die transactionen durchgeführt
